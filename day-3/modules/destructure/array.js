var numbers = [2, 5, 10];
var [x, y, z] = numbers;
console.log(x);
console.log(y);
console.log(z);
var [a,,c] = numbers;

var [one, two, three] = [1, 2, 3];

/*
 *
 * 1. Create an array of at least 5 elements
 *
 * 2. Pull out every element into a local var
 *
 * 3. pull out every other element into a local var
 *
 * 4. use destructure to swap the values of two vars
 */

var num1 = 4;
var num2 = 5;
[num1, num2] = [num2, num1];




