var express = require('express');
var fs = require('fs');
console.log(express);
/*
 * Create a package.json with npm init
 * 
 * install and save a package to it
 * (npm install --save express)
 *
 * create an app.js file that loads this
 * dependency
 */
